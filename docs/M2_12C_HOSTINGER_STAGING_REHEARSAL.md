# M2.12C Hostinger staging rehearsal

Status: **READY** — `M2_12C_HOSTINGER_STAGING_REHEARSAL_READY`.

- Starting SHA: `71968494ce19e055579365a51f49fa47098e30d2`; prior staged SHA `38355d3899e8156200b7d9e6ed9138d07582eafc`; current staged SHA `b69c7e125f083f52dc519f4a3cc3d401ba5a64b0` on `develop`.
- Hostinger target: `u777904340@145.223.108.179:65002` (`id-dci-web1761.main-hosting.eu`).
- Topology: separate Hostinger website `staging-a3.ciptagrafika.com`; private root `/home/u777904340/staging-a3-app`; browser-tested application release `/releases/b69c7e125f083f52dc519f4a3cc3d401ba5a64b0` (prior releases retained for rollback); `current` symlink; public root `/home/u777904340/domains/staging-a3.ciptagrafika.com/public_html`.
- Production root was read-only checked before/after. `default.php` SHA-256 remains `aba5b5856471c610e4dd52c322c7a72a895fc9bf98ac1d027528d0e7de1f7e45`.
- PHP web 8.3.30 (HTTP header), CLI 8.2.30; Composer 2.9.8; required extensions, git, rsync, tar, mysql client and mysqldump available.
- Staging DB only: `u777904340_a3staging`. MariaDB `11.8.8-MariaDB-log`; InnoDB `DEFAULT`; server charset `utf8mb4`; collation `utf8mb4_unicode_ci`; `lower_case_table_names=0`; SQL mode `NO_AUTO_CREATE_USER,NO_ENGINE_SUBSTITUTION`; timezone `SYSTEM`.
- 15 Laravel migrations applied successfully in batch 1. Synthetic `test@example.com` user only; platform-superuser privilege granted with the canonical Artisan command. No operational data imported.
- Shared `.env` is private mode 600, staging APP_KEY present, APP_DEBUG=false, and exact APP_GIT_SHA configured. Storage/bootstrap-cache are mode 750 and writable. Secrets were never printed or committed.
- Frontend was built locally with `VITE_DATA_BACKEND=laravel` and no Supabase frontend credentials, then synced to staging public root. Laravel direct front controller health and version pass over HTTPS (`/index.php/api/v1/health`, `/index.php/api/v1/version`), with exact SHA visible after config cache is cleared.
- Staging `.htaccess` contains API/Sanctum/up exclusions, static-file handling, SPA fallback and `Options -Indexes`. Secret probes are non-200 (`.env` 403, `composer.json` 404).
- **Resolved gate:** The 404 was not a cache object. Direct origin (`LiteSpeed`) and HCDN both returned the same platform 404, cache-busting did not change it, and HCDN reported `DYNAMIC`. The staging `.htaccess` was mode `640` and therefore ignored by LiteSpeed; changing only that staging file to `644` restored clean rewrites. `/`, `/up`, `/api/v1/health`, `/api/v1/version`, `/sanctum/csrf-cookie`, and representative SPA routes now pass at origin and edge. No cache purge was required.
- `config:cache`/`route:cache` pass; `view:cache` required the framework `config/view.php`, which was added only to this staging release. Config cache hides the controller's raw `env(APP_GIT_SHA)` lookup, so config cache is currently cleared for exact-SHA endpoint evidence.
- Repository fix committed and pushed as `38355d3899e8156200b7d9e6ed9138d07582eafc`; `config/view.php` is the exact Laravel 11.56.1 framework default. Inventory fix committed and pushed as `b69c7e125f083f52dc519f4a3cc3d401ba5a64b0`; staging `current` points to that release and `/api/v1/version` reports the exact SHA.
- Staging backup is gzip-valid, mode `600`, SHA-256 `92c53b0384c0a930f76b518c89cb58843904cc329f7cd7fcfa3f29fddc9d4ed7`. It restored into isolated `u777904340_stagingrestore`; source `DEFINER` clauses were removed only in the import stream because the restricted restore user cannot impersonate the staging user. Active `u777904340_a3staging` was not modified. Normalized schema dumps match (45 tables, 489 columns, 286 index entries, 78 foreign keys, 2 triggers); migration ledger and aggregate row counts match.
- Release rollback rehearsal switched `current` to `71968494ce19e055579365a51f49fa47098e30d2`, passed `/api/v1/health` 200, then returned to `b69c7e125f083f52dc519f4a3cc3d401ba5a64b0` and verified `/api/v1/version` 200 with the current SHA.
- Exact application-SHA CI passed for `b69c7e125f083f52dc519f4a3cc3d401ba5a64b0`: Database CI `33470449131`; Laravel MySQL Target CI `33470449136`. Local Laravel tests pass (54 passed, 2 expected skips); frontend build passes. Hostinger readiness passes runtime/filesystem/extension checks and intentionally fails only the production-environment assertion for staging.
- Manual browser verification passed at 390×844, 768px, and 1440px: authenticated login/session, direct navigation, hard refresh, `/daily`, `/machines`, `/inventory` empty state, `/reports`, authorized `/settings`, My Account, responsive account surfaces, drawer behavior, logout, and unauthenticated restoration. Console had zero critical errors; launch-critical application requests had zero failures; CSRF/API same-origin Laravel behavior passed; no API route fell through to SPA HTML; no silent fallback; measured operational Supabase requests were `0`. No production DNS, database, public root, legacy, Vercel, Supabase, or protected branch was changed.

Inventory request `159e9222-26be-481b-bf95-687c2f1d5c6c` was a `QueryException` (`SQLSTATE[42S02]`) caused by `InventoryController` querying nonexistent `components` instead of canonical `component_catalogs`. The smallest fix is deployed and an authorized empty-branch regression passes. Manual retest at 390×844 passed: `/inventory` returned HTTP 200, `STAGING-SMOKE` rendered 0 active items, related Components/Machine Models/Machines requests returned 200, the prior error state disappeared, and no critical mobile clipping was visible.
